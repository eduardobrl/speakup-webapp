import styled from "styled-components";

export default styled.div`
    width: 90%;
    aspect-ratio: 16/9;
    border: 43px solid #EA6250;
    box-shadow: inset 0px 4px 4px rgba(0, 0, 0, 0.25);
    box-sizing: border-box;
    filter: drop-shadow(-7px 19px 23px rgba(0, 0, 0, 0.25));
    border-radius: 26px;
`